package me.earth.pingbypass.api.setting.settings.container;

import org.junit.jupiter.api.Test;

public class ContainerTest {
    @Test
    public void testToJson() {
        /*Container<Nameable> container = new Container<>(Collections.singleton(new NameableImpl("test")),
                                                        Container.Action.CLEAR, new NameableImpl("value"));
        JsonElement element = container.toJson();
        assertTrue(element.isJsonObject());
        System.out.println(element);*/
    }

}
