package me.earth.pingbypass.api.setting.settings;

import org.junit.jupiter.api.Test;

public class ColorSettingTest {
    @Test
    public void testToAndFromJson() {
        /*ColorSetting setting = new ColorSetting("test", Color.RED);
        setting.setRainbow(true);
        setting.setRainbowSpeed(50.0f);
        setting.setRainbowBrightness(20.0f);
        setting.setSync(true);
        setting.setRainbowSpeed(10.0f);

        JsonElement element = setting.toJson();
        ColorSetting setting1 = new ColorSetting("test2", Color.WHITE);
        setting1.fromJson(element);

        assertEquals(setting.isRainbow(), setting1.isRainbow());
        assertEquals(setting.isSync(), setting1.isSync());
        assertEquals(setting.getRainbowBrightness(), setting1.getRainbowBrightness());
        assertEquals(setting.getRainbowSpeed(), setting1.getRainbowSpeed());
        assertEquals(setting.getRainbowSaturation(), setting1.getRainbowSaturation());
        assertEquals(setting.getRed(), setting1.getRed());
        assertEquals(setting.getGreen(), setting1.getGreen());
        assertEquals(setting.getBlue(), setting1.getBlue());
        assertEquals(setting.getAlpha(), setting1.getAlpha());*/
    }

}
